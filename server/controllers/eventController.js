const db = require('../models/database');

function createUser(req, res) {
  console.log('createUser');
}

function getCharityInfo(req, res) {
  console.log('getCharityInfo');
}

module.exports = { createUser, getCharityInfo };